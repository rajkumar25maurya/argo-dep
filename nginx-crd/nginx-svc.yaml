apiVersion: v1
kind: Service
metadata:
  name: nginx-crd
  namespace: nginx-crd
spec:
  type: NodePort
  selector:
    app: nginx-crd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082