apiVersion: v1
kind: Service
metadata:
  name: nginx-crd-service
  namespace: nginx-crd
spec:
  type: NodePort
  selector:
    app: nginx-crd-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30083